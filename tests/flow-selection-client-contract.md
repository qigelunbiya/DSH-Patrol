Manual verification target:

- blank Patrol conversation shows “选择流程” instead of a direct-run label
- clicking a flow highlights it and displays metadata plus the ordered flow graph
- clicking a flow does not send a conversation message
- “执行选中流程” stays disabled until a flow is selected
- execution starts only after the explicit execute button is pressed
