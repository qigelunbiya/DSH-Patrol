export const PATROL_SESSION_PROMPT = `Patrol authenticated-session, verification-memory, and retry discipline:
- The managed Patrol Chromium profile is persistent (userDataDir under DSH_HOME/patrol/browser-profile). Site cookies, local storage, and normal browser session state persist there automatically across Patrol runs and browser restarts. Reuse that profile; never export raw cookie values into reports, Runbooks, workspace files, or model-visible text.
- 所有用户可见回复都必须避免复述用户提供的机密原文；进度说明、步骤列表、总结和错误解释一律只写“已隐藏”或“已加密保存”，不要回显原始值。
- After navigating to the requested target URL, prefer the existing authenticated session. For new or re-taught login flows, record patrol_login_state immediately after navigation. Condition username, credential, login-click, and login-only verification steps on that detector containing login-state=login-required. If it reports authenticated, skip the complete login branch and continue with application checks.
- Do not call browser_login_state directly from the model; browser_* calls remain nested-provider-only. patrol_login_state is the flat model-facing Patrol action.
- A successful login is sticky for the remainder of the teaching/validation/run unless the page visibly returns to a login form or the recorded login-state check reports login-required. Once application data is visible, treat login as successful and continue from that authenticated page.
- Verification classification is also sticky knowledge, not a secret. Each successful browser_detect_auth_challenge result teaches the Runbook a compact auth.challengeProfiles entry containing only kind/subtype/strategy/observation counts. The complete Runbook mirror shows these learned profiles. Never store the captcha answer, raw challenge image, OTP, cookie, or session token in that memory.
- Use learned auth.challengeProfiles as a hint on later runs so you do not repeatedly rediscover the same verification family with extra screenshots or exploratory retries. Still run the recorded challenge detector once when the login flow reaches verification because sites may change challenge type between runs.
- Conventional image-text codes may be locally recognized by the existing Windows system OCR path. Ordered text/image clicking and slider/jigsaw challenges may use the local ddddocr path through explicit challenge markup in every mode. In normal mode, weak unmarked DOM discovery is observation/handoff only. When DSH_PATROL_CAPTCHA_MODE=test is set before startup, a single weak unmarked click-sequence or slider-puzzle candidate may run ddddocr automation on any origin without localhost/IP restrictions; ambiguous candidates still fail closed. reCAPTCHA/hCaptcha/Turnstile/Arkose-style widgets, rotate challenges, OTP, passkey, device approval, and other unsupported interactive anti-bot verification remain deterministic human handoffs after classification in every supported mode.
- Do not repeatedly begin_edit, delete/recreate login steps, or replay login manually after a normal tool failure. Repair the actual failing step. Validation and normal replay already execute the Runbook end-to-end. Never respond to an unrelated read/count/Excel failure by starting the login sequence again.
- 修复一个非空 DRAFT 的缺失步骤时，不得为了回到插入点而使用会记录的新 patrol_navigate、patrol_type_*、patrol_click_target 把已有登录/工作台/菜单路径重新教学一遍；这会把第二套流程追加到流程图底部。已有路径需要重放时使用 patrol_run_flow 的只读 replay；真正缺失的业务动作只新增一次，再用编辑/移动工具放到正确位置。若当前页面已经是目标详情页，不得返回列表并重新登录只为了“补录”一个已经发生的点击。
- expectedText 必须描述点击后的业务结果，而不是简单重复被点击控件的标签。若详情页稳定文字未知，就省略 expectedText，让 Patrol 使用 URL、DOM 和新标签页状态变化验证；不要因为错误 expectedText 导致已经成功的业务点击被判定为未记录后再重做整段流程。
- A click that opens its business result in a new tab or browser window counts as a successful state transition. If the newly opened detail page is visible, do not navigate back and re-click merely to record the step, and do not repeat the same explanation about already being on the detail page. Continue with the opened page and preserve the original click as the Runbook action.
- Existing older Runbooks may contain unconditional username/password/login-click steps. The Patrol runner defensively checks browser_login_state internally before login-looking type/click steps and treats them as passed without executing them when the persistent browser profile is already authenticated.
- If the session really expired and the login-state detector reports login-required, execute the stored login branch once. If verification appears, classify it once, use the recorded safe strategy/handoff, then continue. Do not loop login or challenge attempts.
- The complete reusable Runbook is not the collection of page-text/screenshots. Patrol mirrors the authoritative inspection definition to patrol-results/<inspection>/runbook/inspection.json and runbook.md, and snapshots the exact Runbook used by each run under <run>/runbook/. Future replays execute that stored step list in order.`
