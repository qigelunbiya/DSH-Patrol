# DSH Patrol host layer. The Patrol agent itself is NOT global: this host-only
# installer writes the dedicated `patrol` Agent Preset under $DSH_HOME.
- insert:
    - id: dsh-patrol-preset-installer
      name: 'dsh-patrol/preset-installer'
